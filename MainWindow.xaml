<!-- MainWindow.xaml - Beautiful UI with all the styling -->
<Window x:Class="WebView2Browser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Modern WebView2 Browser" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#1e1e1e">
    
    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#404040"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tab Button Style -->
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="1,0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MaxWidth" Value="200"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8,8,0,0" 
                                BorderThickness="0">
                            <Grid>
                                <TextBlock Text="{TemplateBinding Content}" 
                                         HorizontalAlignment="Left" 
                                         VerticalAlignment="Center"
                                         Margin="8,0,25,0"
                                         TextTrimming="CharacterEllipsis"/>
                                <Button Name="CloseButton" 
                                       Content="×" 
                                       HorizontalAlignment="Right" 
                                       VerticalAlignment="Center"
                                       Width="20" Height="20" 
                                       Margin="0,0,5,0"
                                       Background="Transparent" 
                                       Foreground="#ffffff"
                                       BorderThickness="0"
                                       FontSize="14"
                                       FontWeight="Bold"
                                       Cursor="Hand"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#404040"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Active Tab Button Style (Blue accent) -->
        <Style x:Key="ActiveTabButtonStyle" TargetType="Button" BasedOn="{StaticResource TabButtonStyle}">
            <Setter Property="Background" Value="#0078d4"/>
        </Style>

        <!-- URL TextBox Style (Rounded) -->
<Style x:Key="UrlTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#3c3c3c"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border" 
                                CornerRadius="20" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#0078d4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tab Bar (Dark Gray Background) -->
        <Grid Grid.Row="0" Background="#252525" Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <ScrollViewer Grid.Column="0" 
                         HorizontalScrollBarVisibility="Auto" 
                         VerticalScrollBarVisibility="Hidden"
                         Background="Transparent">
                <StackPanel x:Name="TabPanel" 
                           Orientation="Horizontal" 
                           VerticalAlignment="Center"
                           Margin="10,0"/>
            </ScrollViewer>
            
            <!-- New Tab Button (+) -->
            <Button Grid.Column="1" 
                   Content="+" 
                   Style="{StaticResource ModernButtonStyle}"
                   Width="30" Height="30"
                   Margin="5"
                   FontSize="16"
                   FontWeight="Bold"
                   Click="NewTab_Click"/>
        </Grid>

        <!-- Navigation Bar (Darker Gray) -->
        <Grid Grid.Row="1" Background="#2d2d2d" Height="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Back Button -->
            <Button Grid.Column="0" 
                   Content="←" 
                   Style="{StaticResource ModernButtonStyle}"
                   Width="40" Height="35"
                   FontSize="16"
                   Click="GoBack_Click"
                   x:Name="BackButton"/>
            
            <!-- Forward Button -->
            <Button Grid.Column="1" 
                   Content="→" 
                   Style="{StaticResource ModernButtonStyle}"
                   Width="40" Height="35"
                   FontSize="16"
                   Click="GoForward_Click"
                   x:Name="ForwardButton"/>
            
            <!-- Refresh Button -->
            <Button Grid.Column="2" 
                   Content="⟳" 
                   Style="{StaticResource ModernButtonStyle}"
                   Width="40" Height="35"
                   FontSize="16"
                   Click="Refresh_Click"/>

            <!-- URL Bar (Rounded with Blue Focus) -->
            <TextBox Grid.Column="3" 
                    x:Name="UrlTextBox"
                    Style="{StaticResource UrlTextBoxStyle}"
                    Margin="10,7.5"
                    KeyDown="UrlTextBox_KeyDown"/>

            <!-- Home Button -->
            <Button Grid.Column="4" 
                   Content="🏠" 
                   Style="{StaticResource ModernButtonStyle}"
                   Width="40" Height="35"
                   FontSize="14"
                   Click="Home_Click"/>
            
            <!-- Menu Button -->
            <Button Grid.Column="5" 
                   Content="⋮" 
                   Style="{StaticResource ModernButtonStyle}"
                   Width="40" Height="35"
                   FontSize="16"
                   Click="Menu_Click"/>
        </Grid>

        <!-- WebView Container (Main Content Area) -->
        <Grid Grid.Row="2" x:Name="WebViewContainer" Background="#1e1e1e">
            <!-- WebView2 controls will be added here dynamically -->
        </Grid>
    </Grid>
</Window>